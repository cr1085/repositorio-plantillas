import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cbsoluciones.webtemplate',
  appName: 'web-templates',
  webDir: 'www'
};

export default config;
